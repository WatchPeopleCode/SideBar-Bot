{
	"bot": {
		"username": "YOUR BOTS USERNAME",
		"password": "YOUR BOTS PASSWORD"
	},
	"subreddit": "The subreddit, note: the bot MUST have mod permissions",
	"mode": "EITHER 'live', 'upcoming' or 'completed'",
	"description": {
		"pre": "Markdown to be appended before the streams",
		"viewers_template": "Use a '{}' for the viewer variable. For example: 'There are {} people watching all the streamers` ",
		"template": "Use three '{}' for the variables in the order username, title, url. For example: '{} is streaming [{}]({}) to {} viewers' ",
		"post": "Markdown to be appended after the streams"
	},
	"timer": 300,
	"debug": true
}
